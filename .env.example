# Database
POSTGRES_PASSWORD = "postgres"
POSTGRES_USER = "postgres"
POSTGRES_DB = "postgres"
POSTGRES_HOST = "db"
POSTGRES_PORT = "5432"

DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# Redis (Bull)
BULL_DB_HOST = "bull-db"

# Use Redis DB for keep track the current download in progress
CURRENT_DOWNLOADS_IN_PROGRESS_DB_HOST = "current-downloads-in-progress-db"

# APP
NODE_ENV = "development"
PORT = "3000"
TZ = 'America/Recife'
